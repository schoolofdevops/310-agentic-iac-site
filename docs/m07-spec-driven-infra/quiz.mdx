---
sidebar_position: 5
title: 'Quiz'
---

import Quiz from '@site/src/components/Quiz';

# M07 Quiz: Spec-Driven Infrastructure

<Quiz questions={[
  {
    prompt:'What does a written spec have that a one-line intent doesn\'t?',options:[{text:'Better grammar',correct:false,explanation:'Grammar isn\'t the point.'},{text:'Requirements, constraints, and acceptance criteria, stated before generation',correct:true,explanation:'A one-line intent has a goal and nothing else. A spec adds three specific, checkable parts, written down before any code exists.'},{text:'A longer file',correct:false,explanation:'Length isn\'t the point.'},{text:'A guarantee that checkov passes clean',correct:false,explanation:'A spec never guarantees a fully clean scanner run, it only covers what its author wrote down.'}]
  },
  {
    prompt:'A ticket says "give me an S3 bucket for storing build artifacts." Generated with no spec, iterated on by feel until it looks done. What is this?',options:[{text:'Spec-driven development',correct:false,explanation:'Spec-driven work writes requirements, constraints, and criteria first. This has none of that.'},{text:'Vibe coding',correct:true,explanation:'Generate, eyeball, tweak, eyeball again, with no written target and no fixed acceptance criteria. Named once in this course, as a failed approach on infrastructure specifically.'},{text:'A gate',correct:false,explanation:'A gate blocks apply based on something mechanical. This is about how the module was generated, not a gate.'},{text:'Policy as code',correct:false,explanation:'Policy as code is a scanning stage, not a generation style.'}]
  },
  {
    prompt:'Why does vibe coding fail on infrastructure specifically, more than it fails on application code?',options:[{text:'Infrastructure code is harder to read',correct:false,explanation:'Readability isn\'t the asymmetry this course names.'},{text:'Terraform has stricter syntax than most languages',correct:false,explanation:'Syntax strictness isn\'t the reason.'},{text:'No undo, and failure that stays quiet until someone looks, the same asymmetries from M01',correct:true,explanation:'Application code that\'s close enough usually gets fixed in the next release. Infrastructure that\'s close enough is often already live, holding real data, before anyone reads it carefully.'},{text:'Infrastructure teams don\'t use version control',correct:false,explanation:'Version control usage isn\'t the issue here.'}]
  },
  {
    prompt:'A spec\'s acceptance criteria list three things: versioning, public-access-block, and encryption. Checkov still finds 5 more failures on the generated module. What does this mean?',options:[{text:'The spec was written badly and should be rejected',correct:false,explanation:'A narrower spec than checkov\'s full ruleset isn\'t automatically a bad spec.'},{text:'Checkov is buggy',correct:false,explanation:'Checkov is working exactly as designed, checking a ruleset wider than this one spec.'},{text:'The spec did its job, it just didn\'t cover everything checkov checks, which is exactly why the policy gate in M09 still runs afterward',correct:true,explanation:'A spec\'s acceptance criteria only cover what its author thought to write down. That gap is the whole reason M09\'s policy check exists as a separate stage.'},{text:'The module should be regenerated until checkov passes fully clean',correct:false,explanation:'That treats the spec as if it should replace the scanner, which was never the plan.'}]
  },
  {
    prompt:'Given the course thesis "the agent proposes, the pipeline decides," where does a spec fit?',options:[{text:'It replaces the gate in M06, since a good spec means apply is already safe',correct:false,explanation:'Apply still needs a gate whether the module came from a careful spec or from guessing.'},{text:'It shapes the ask, before generation happens at all, it doesn\'t block or scan anything itself',correct:true,explanation:'A spec is nothing but a better question, asked before generation starts. The gate and the policy check are separate, later stages.'},{text:'It\'s the same thing as a policy check, just written earlier',correct:false,explanation:'A spec and a policy check are different jobs at different moments.'},{text:'It only matters for Tier 0 exercises',correct:false,explanation:'Real reviewers and real consequences apply well beyond Tier 0.'}]
  },
  {
    prompt:'A team lead says: "We don\'t need specs, we have checkov in CI." What\'s the gap in that argument?',options:[{text:'Checkov is slower than writing a spec',correct:false,explanation:'Speed isn\'t the gap.'},{text:'Checkov catches what\'s in its ruleset after generation, it can\'t shape what gets generated in the first place, and it won\'t catch an org-specific rule nobody encoded',correct:true,explanation:'Checkov runs after the fact against its own ruleset. A spec shapes the ask before generation; checkov checks the result afterward.'},{text:'Specs and checkov do the exact same job',correct:false,explanation:'They\'re different jobs at different moments, that\'s the point being missed.'},{text:'Checkov only works on Terraform, not other languages',correct:false,explanation:'Language support isn\'t the gap in this argument.'}]
  },
  {
    prompt:'A throwaway Tier 0 exercise, nobody will review it, nothing depends on it. Do you need a written spec?',options:[{text:'Yes, always write a spec, no exceptions',correct:false,explanation:'A spec costs real time. That cost isn\'t always worth paying.'},{text:'No, a one-line intent is enough here, the overhead is only worth it once a real reviewer or a real consequence enters the picture',correct:true,explanation:'A throwaway exercise nobody else will ever look at doesn\'t clear the bar for writing a spec.'},{text:'No, specs are only for Tier 2 and Tier 3 labs',correct:false,explanation:'This isn\'t a lab-tier rule, it\'s about reviewers and consequences.'},{text:'Yes, but only if the exercise uses Terraform',correct:false,explanation:'This isn\'t a tool-specific rule.'}]
  }
]} />
