---
sidebar_position: 5
title: 'Quiz'
---

import Quiz from '@site/src/components/Quiz';

# M02 Quiz: Your Agentic IaC Workstation

<Quiz questions={[
  {
    prompt:'Why does this course teach both Claude Code and Codex CLI instead of picking one?',options:[{text:'Because one of them will be discontinued soon',correct:false,explanation:'Neither tool\'s future is the reason, this is about what actually transfers between agent CLIs.'},{text:'Because a team that standardizes on one tool still needs to read code the other tool produced, and the underlying loop transfers between both',correct:true,explanation:'Both tools run the same core loop from module 1: intent, act, observe, decide, repeat, stop. Teaching only one CLI would teach that CLI\'s quirks instead of the loop underneath it.'},{text:'Because Codex CLI is required for Tier 1 labs',correct:false,explanation:'Tier 1 labs use Floci, not a specific agent CLI.'},{text:'Because Claude Code cannot run Terraform',correct:false,explanation:'Claude Code can run Terraform commands fine, that\'s not the reason.'}]
  },
  {
    prompt:'Why does this course have you install Terraform, Checkov, and Trivy at exact pinned versions instead of "whatever\'s newest"?',options:[{text:'Newer versions cost money',correct:false,explanation:'Cost has nothing to do with it, every one of these tools is free.'},{text:'Every [ Expected output ] block in this course was captured from a real run against those exact versions, so a different version on your machine can genuinely produce different output',correct:true,explanation:'Pinned tool versions mean "it works on my machine" isn\'t a debugging step, it\'s a bug, because your machine is running the exact versions the course\'s captured output came from.'},{text:'Pinned versions run faster than the latest release',correct:false,explanation:'Speed is not the reason these exact versions are pinned.'},{text:'Older tool versions are required for Docker to work at all',correct:false,explanation:'Docker itself has no dependency on these specific Terraform, Checkov, or Trivy versions.'}]
  },
  {
    prompt:'An agent proposes Terraform as chat text only, and a human types it into the file by hand. Which step on the autonomy ladder is this?',options:[{text:'Step 1, suggest',correct:true,explanation:'The agent never touches a file. Every keystroke that lands in the repo is the human\'s, even though the words came from the agent.'},{text:'Step 2, draft',correct:false,explanation:'Step 2 is when the agent writes the file directly, not when a human retypes a suggestion.'},{text:'Step 3, propose with plan',correct:false,explanation:'Step 3 needs both code and a plan produced together, not a chat-only suggestion.'},{text:'Step 4, gated apply',correct:false,explanation:'Step 4 involves automated checks plus human approval before an apply, well past a chat suggestion.'}]
  },
  {
    prompt:'An agent writes main.tf directly to disk, and a human reads it before running anything. Which step is this?',options:[{text:'Step 1, suggest',correct:false,explanation:'Step 1 never touches a file directly, this scenario does.'},{text:'Step 2, draft',correct:true,explanation:'The difference from step 1 isn\'t "more trust," it\'s a different kind of control: from typing every character yourself to reading every line before it runs.'},{text:'Step 5, supervised',correct:false,explanation:'Step 5 has the agent looping across multiple iterations on its own, not a single file write reviewed once.'},{text:'Step 6, unattended',correct:false,explanation:'Step 6 has no human reviewing each step at all, this scenario has a human reading before anything runs.'}]
  },
  {
    prompt:'In this module\'s own real, captured run, the same one-line intent, run twice through the same agent, produced two different Terraform designs. What does that tell you?',options:[{text:'The agent made a mistake and one of the two runs is wrong',correct:false,explanation:'Both designs were individually reasonable against the intent, neither was simply wrong.'},{text:'Each session starts cold with no memory of a prior run, so reading every line every time matters, not just on the first run',correct:true,explanation:'Unless something gives an agent memory of a prior session on purpose, nothing carries over. Two runs of the same ask can both be individually reasonable and still diverge.'},{text:'This only happens with Codex, never with Claude Code',correct:false,explanation:'The real captured divergence in this module happened using the same tool across both runs.'},{text:'It means one of the two Terraform versions is misconfigured',correct:false,explanation:'Terraform version pinning has nothing to do with an agent\'s session memory.'}]
  },
  {
    prompt:'What\'s in CLAUDE.md and AGENTS.md at the end of this module?',options:[{text:'Full provider pins and naming conventions',correct:false,explanation:'That real content gets written in module 3, not this one.'},{text:'A list of every skill available to the agent',correct:false,explanation:'Agent Skills are module 4\'s subject, not what these files hold here.'},{text:'Nothing yet, they\'re empty, real content goes in during module 3',correct:true,explanation:'This module only shows where each tool\'s standing-context file lives. Writing real content into them is module 3\'s subject, context engineering.'},{text:'The learner\'s personal API keys',correct:false,explanation:'Neither file is meant to hold credentials.'}]
  },
  {
    prompt:'This module\'s own lab found a real bug: two independent agent sessions both wrote a docker_container volume host_path that referenced path.module without wrapping it in abspath(). terraform validate passed both times. What does that actually prove?',options:[{text:'terraform validate is broken and shouldn\'t be trusted at all',correct:false,explanation:'validate still does real, useful work, checking syntax and types, it just doesn\'t check every provider-enforced runtime constraint.'},{text:'terraform validate checks syntax and types, not every constraint a provider enforces once it plans a real change, so a clean validate is not the same as a clean plan',correct:true,explanation:'terraform plan caught what validate couldn\'t: the docker provider rejects a relative host_path once it actually has to act on it.'},{text:'The bug only exists because Docker is misconfigured',correct:false,explanation:'The bug is in the Terraform file\'s path reference, not in how Docker itself is set up.'},{text:'abspath() is optional, the module would have applied fine either way',correct:false,explanation:'Both real runs validated clean and both would still have failed the moment anyone tried to plan or apply them, without abspath().'}]
  },
  {
    prompt:'In the lab\'s acceptEdits step, a subagent\'s checkov call got blocked by its own tool permissions, and it told you to run it yourself instead of guessing. Why is that the right behavior, not a bug?',options:[{text:'Subagents should always have every permission the parent session has',correct:false,explanation:'That would remove the isolation a subagent is meant to provide.'},{text:'A subagent\'s narrower permission surface, and reporting a block honestly instead of fabricating a result, is exactly the isolation a bounded delegated task should have',correct:true,explanation:'Delegating to a subagent is meant to bound what a task can touch, not hand it a blank check on your own permissions.'},{text:'It\'s a bug, the subagent should have used --dangerously-skip-permissions',correct:false,explanation:'That would defeat the entire purpose of delegating a bounded, read-only audit.'},{text:'Subagents can never run Bash commands under any configuration',correct:false,explanation:'Subagents can run Bash when granted the permission, this one simply wasn\'t granted a wide enough pattern.'}]
  },
  {
    prompt:'What does a custom slash command in .claude/commands/ actually do that typing the same prompt again doesn\'t?',options:[{text:'It makes the agent smarter',correct:false,explanation:'A slash command doesn\'t change model capability, it packages a repeatable instruction.'},{text:'It turns a repeatable sequence into a fact the repo carries, checked in alongside the code it applies to, so anyone or any agent working in that directory gets the same floor',correct:true,explanation:'/tf-check runs the same fmt/init/validate/plan sequence for anyone who opens this repo, the same way AGENTS.md carries a rule instead of a person carrying it in their head.'},{text:'It grants the agent extra tool permissions automatically',correct:false,explanation:'A slash command is a prompt template, it doesn\'t modify --allowedTools on its own.'},{text:'It only works with Codex, not Claude Code',correct:false,explanation:'Slash commands in .claude/commands/ are a Claude Code feature, demonstrated in this module.'}]
  }
]} />
